import { Security, Session } from '../client';
import { StoreParams } from './../filelink';
/**
 *
 * @private
 * @param session
 * @param url
 * @param opts
 * @param token
 * @param security
 */
export declare const storeURL: (session: Session, url?: string, opts?: StoreParams, token?: any, security?: Security) => Promise<any>;
